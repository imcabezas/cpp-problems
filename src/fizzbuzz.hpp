#pragma once

#include <string>
#include <functional>

// This function implements the FizzBuzz logic using a naive approach.
std::string fizz_buzz_naive(unsigned int n);

// This function implements the FizzBuzz logic using a more efficient approach.
std::string fizz_buzz_function(unsigned int n);
